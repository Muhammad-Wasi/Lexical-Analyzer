{
  "name": "lexicalanalyzer",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "watch": "nodemon server.js",
    "start": "node server.js",
    "prettier:config": "npx prettier --ignore-path .gitignore --config .prettierrc '**/*.{js,json,md,yml,yaml,css,scss,html,vue}'",
    "prettier:fix": "npm run prettier:config -- --write ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "fs": "^0.0.1-security",
    "https": "^1.0.0",
    "lexical-parser": "^1.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15",
    "prettier": "^2.5.1"
  }
}
